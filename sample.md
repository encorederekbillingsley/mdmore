# mdmore Sample

A quick tour of **bold**, *italic*, ~~strikethrough~~, and `inline code`.

## Lists

- First bullet with a fairly long line so we can confirm that word wrapping kicks in at the configured console width and continuation lines line up under the text.
- Second bullet
  - Nested bullet
  - Another nested one
- Third bullet

1. Ordered one
2. Ordered two
3. Ordered three

## A blockquote

> Markdown is intended to be as easy-to-read and easy-to-write as is feasible.
> Readability is a prime concern.

## Code

```csharp
var greeting = "hello";
Console.WriteLine(greeting);
```

## A table

| Feature   | Supported |
|-----------|-----------|
| Headings  | yes       |
| Tables    | yes       |
| Links     | yes       |

## Heading with `inline code` and a [link](https://example.com)

Nested styling: a [link to `code`](https://example.com), **bold with `code` inside**, and *italic `code`*.

## Links

See the [Markdig project](https://github.com/xoofx/markdig) for details.

---

That's all.
